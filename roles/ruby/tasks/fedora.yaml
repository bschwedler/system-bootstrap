---
- name: Install Ruby
  ansible.builtin.dnf:
    name:
      - ruby
      - ruby-devel
      - rubygem-json
    state: "{{ package_state }}"

- name: Install Bundler.
  gem:
    name: bundler
    state: "{{ package_state }}"
    user_install: no
