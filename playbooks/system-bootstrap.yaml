---
- hosts:
  - localhost
  vars:
    # Pass 'latest' via command line to upgrade packages
    package_state: present

  roles:
    - role: packagemanager
      tags:
        - packagemanager
        - pkg
        - dnf
        - yum
    - role: utils
      tags:
         - utils
    - role: libraries
      tags:
        - libraries
        - lib
